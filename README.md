# TmapNda

콤마(openpilot)의 가감속 제어를 위한 **Bridge 앱**입니다.
콤마 기본 안전운전 모드를 기반으로, Tmap·카카오내비에서 받아온 도로 정보(제한속도, 과속카메라, 구간단속, 방지턱 등)를 콤마에 전달해 길 상황에 맞게 속도를 조절하도록 도와줍니다.

화면 상단에 정보 바(속도/제한속도/openpilot 연결상태/GPS 정확도/검색창/메뉴)가 항상 떠 있고, 목적지를 검색하면 카카오내비 턴바이턴 길안내로 자연스럽게 전환됩니다. Tmap 화면과 카카오 길안내 화면은 동일한 UI로 통일되어 있습니다.

---

## 시작하기 전에

- TmapNda 실행에는 [openapi.sk.com](https://openapi.sk.com)에서 발급받은 **Tmap AppKey**가 필요합니다.
- 목적지 검색·카카오 길안내를 쓰려면 **카카오 REST API 키 + Default Native AppKey**도 필요합니다 (아래 2번 참고).
- 순정 Tmap이나 차량 순정 내비게이션과 **충돌하지 않습니다.** 어떤 내비를 쓰든 TmapNda는 독립적으로 켜지고 동작합니다.
- TmapNda는 콤마와 **같은 IP 대역(같은 네트워크)** 에 있어야 합니다. 즉 TmapNda가 실행 중인 폰(또는 엔미러) → 네트워크 ↔ 콤마가 연결되어야 합니다.
  - 폰에 저장된 와이파이가 여러 개면, 신호가 더 세거나 우선순위가 높은 다른 네트워크로 자동 전환되면서 콤마와 다른 네트워크에 있게 되는 경우가 있습니다. **콤마와 항상 연결할 네트워크 하나만 폰에 남기고 나머지는 삭제(잊기)** 하는 걸 권장합니다.
- 정상 연결되면 상단바에 `Comma 연결됨`과 `크루즈 작동중`/`크루즈 대기중`/`크루즈 불안정` 상태가 표시됩니다.
  - `Comma 연결됨`(연결 여부)과 `크루즈 작동중`/`크루즈 대기중`(openpilot 자체 조향·가속 제어가 실제로 켜져있는지)는 서로 다른 정보입니다. 연결은 됐는데 크루즈가 대기중인 상태(예: 아직 크루즈를 안 켠 상태)는 정상입니다.
  - openpilot이 보내는 상태가 아주 짧은 주기로 여러 번 바뀌는 경우, 화면 깜빡임 대신 `크루즈 불안정`으로 표시됩니다.

---

## 1. Tmap AppKey 발급받기

1. [openapi.sk.com](https://openapi.sk.com) 접속 (PC 권장)
2. 우측 상단 **로그인** 클릭 → 보유한 **Tmap 계정**으로 로그인 (별도 회원가입 불 필요)
3. 로그인 후 **대시보드** 이동
4. 좌측(또는 상단) 메뉴에서 **앱** 클릭
5. **앱 만들기** 버튼 클릭 → 활성화 확인 ( 비 활성화 시 만든 앱 → 설정 → 앱 상태 → 활성화 )
6. 아래 항목 입력
   | 항목 | 값 |
   |---|---|
   | 앱명 | 자유 입력 (예: 내차TmapNda) |
   | 이용 API | `Tmap` 선택 |
   | 사용요금제 | `무료` 선택 |
   | 이용약관 | 필수 항목 전부 동의 |
7. PRODUCTS → 메뉴클릭 → API 사용요금 → Free 선택 → **신청하기** → **앱 신청** 클릭
8. 앱 목록에서 방금 만든 앱 클릭 (Tmap 무료 API는 보통 즉시~몇 분 내 승인)
9. 앱 상세 페이지에서 **APP Key** 옆 복사 아이콘으로 전체 복사
10. TmapNda 앱 최초 실행 시 뜨는 초기 화면(안전운행 시작하기 화면)의 앱키 입력란에 붙여넣기 → 저장 (이미 한 번 실행했다면 상단바 ≡ 메뉴 → **설정을 길게 누르면** 이 화면으로 다시 이동합니다)
11. TmapNda 앱 완전 종료 후 재실행 → 상단바에 `Comma 연결됨` 표시 확인

> 안 될 때 체크 순서: ① 앱키 복사 시 누락 글자 없는지 → ② 폰과 콤마가 같은 네트워크인지 → ③ TmapNda 재실행했는지

---

## 2. Kakao API 키 발급받기 (목적지 검색·길안내에 필수)

목적지 검색과 카카오내비 턴바이턴 길안내를 쓰려면 아래 키가 필요합니다.

1. [developers.kakao.com](https://developers.kakao.com) 접속 (PC 권장)
2. 우측 상단 **로그인** → 카카오 계정으로 로그인
3. 상단 메뉴 **내 애플리케이션** 이동
4. **애플리케이션 추가하기** 클릭
5. 추가한 앱 클릭 ( Ex. TmapNda )
6. 9번 참고
7. 팝업에서 아래 항목 입력 후 **저장**
   | 항목 | 값 |
   |---|---|
   | 앱 아이콘 | 선택 사항 (생략 가능) |
   | 앱 이름 * | 자유 입력 (예: TmapNda) |
   | 사업자명(회사명) * | 개인이면 이름 등 아무 문자열 |
8. 생성된 앱 카드 클릭해서 상세 화면 진입 (별도 심사 없이 즉시 생성)
9. **아래 두 가지를 모두 진행해야 합니다.** 하나라도 빠지면 검색·길안내가 동작하지 않습니다.

   **① 플랫폼(앱 키) 등록**
   - 좌측 메뉴 **앱 → 앱 키**(또는 **플랫폼 키**) 클릭
   - Default Native AppKey 클릭
   - 아래 값을 입력하고 **저장**

     | 항목 | 값 |
     |---|---|
     | 패키지명 | `com.tmap.nda` |
     | 키 해시 | `zQSvbNffsYGnbQksdHmIUbe10V0=` |

   **② 카카오맵 쿼터 사용 체크**
   - 좌측 메뉴 **앱 → 카카오맵** 클릭
   - **쿼터 사용** 체크박스 켜고 **저장**

10. 필요한 키 복사
   | 키 종류 | 용도 | REST API 키 | 목적지 검색(장소 검색) 호출에 필요 | Default Native AppKey | 카카오내비 SDK(길안내) 초기화에 필요
11. TmapNda 앱 최초 실행 시 뜨는 초기 화면의 키 입력란에 붙여넣기 → 저장 (이미 한 번 실행했다면 상단바 ≡ 메뉴 → **설정을 길게 누르면** 이 화면으로 다시 이동합니다)

---

## 3. 화면 구성

- **상단바** (Tmap/카카오 화면 공통): 속도 · 도로제한속도 · Comma 연결상태(Comma 연결됨/대기중) · 크루즈 상태(크루즈 작동중/대기중/크루즈 불안정) · GPS 정확도 · 음소거 · 목적지 검색(+최근검색) · 집,회사 ( 바로가기 ) · 로그 전송 · ≡ 메뉴 · (카카오 길안내 중엔 안내종료 버튼) · 이벤트(과속카메라·구간단속·방지턱·어린이보호구역·톨게이트 등 24종, 아이콘과 함께 뜰 때만 표시)
  - 화면이 좁으면 상단바가 좌우로 스크롤됩니다.
- **≡ 메뉴(더보기)**: UI 편집 · 업데이트 확인 · 업데이트 내역 · 로그 전체 삭제 · 설정 · 도움말 · 앱 종료
  - 메뉴 버튼을 누르면 그 버튼 바로 아래에 팝업으로 뜨고, 팝업 바깥을 탭하면 자동으로 닫힙니다.
- **UI 편집**: 상단바를 원하는 위치로 자유롭게 드래그해서 옮길 수 있습니다. 편집모드에서는 상단바 아무 곳이나 눌러서 이동 가능하고, 우측 상단에 뜨는 체크(✓) 버튼을 누르면 위치가 저장되고 편집이 끝납니다.
- **설정**: 아래 항목을 켜고 끌 수 있습니다.
  - 속도 10% 초과 시 경고음
  - 이동식카메라 감속 끄기
  - 상단바에 이벤트(카메라/구간단속/방지턱 등) 표시
  - 차선 안내 오버레이 표시 (Tmap 화면 한정 - 카카오 화면에는 표시되지 않습니다)
  - 티맵 위성지도 보기
  - 티맵 교통 정보 (도로 정체 색깔 표시)
  - 즐겨찾기 표시 개수 (-/+ 버튼으로 0~5개)
  - 티맵 터치 잠금 해제 (핀치줌/드래그 허용, Tmap 화면 한정)
- **차선 안내 오버레이**: 카카오 SDK가 만든 추천 차선 정보(좌회전/우회전/직진/유턴 화살표까지 정확 표시)와 버스전용차로 표시를 **Tmap 화면 위에** 오버레이로 띄웁니다. 카카오 길안내 화면 자체에는 표시되지 않습니다(카카오 SDK가 이미 자체적으로 보여주기 때문).
- **차량 계기판/HUD(클러스터) 연동**: Android Auto를 통해 차량 계기판에 회전·거리·ETA 정보를 전송하는 기능이 구현되어 있습니다. 다만 폰이 Android Auto로 차량에 연결되는 방식(미러링)에서만 작동하며, 헤드유닛에 앱을 직접 설치해 쓰는 방식에서는 이 기능이 실행되지 않습니다. 아직 실제 차량에서 정상 동작이 확인되지 않은 기능입니다.

---

## 4. 목적지 검색

- 검색창에 입력하면 카카오 장소 검색 결과가 최대 50건까지(15건씩 최대 4번 추가 조회) 나오고, 10개씩 페이지로 나눠서 "이전 / 다음 / 취소" 버튼으로 넘겨볼 수 있습니다. 결과가 없는 첫/마지막 페이지에서는 해당 버튼이 흐리게 비활성화됩니다.
- 정렬 순서는 **① 검색어와 완전히 같은 이름 → ② 검색어로 시작하는 이름(부속시설류, 그중 이름이 짧은 쪽 우선) → ③ 검색어 글자가 순서대로(붙여쓰기 포함) 들어있는 이름 → ④ 그 외**로 나눠지고, 마지막엔 현재 위치 기준 가까운 순으로 정리됩니다.
- 스타벅스·맥도날드처럼 **DT(드라이브스루) 매장은 이름 길이와 상관없이 항상 위쪽으로, 그 안에서는 가까운 순**으로 뜹니다.
- "편의점", "주유소", "약국", "병원", "카페", "은행", "마트", "주차장" 등 종류로 검색하면 카카오 종류 전용 검색으로 자동 전환되어 더 정확하게 가까운 순으로 나옵니다.
- 검색 결과 각 항목 옆에 현재 위치 기준 거리와 예상 소요시간이 같이 표시됩니다(1km 미만은 미터, 이상은 소수점 한 자리 킬로미터 · 계산 부담을 줄이기 위해 지금 보이는 페이지만 계산).
- 검색창을 직접 눌러 타이핑할 땐 키보드만 뜨고, "최근검색" 아이콘을 눌렀을 때만 검색 이력 팝업이 뜹니다.
- 목록에서 목적지를 고르면, 바로 안내가 시작되지 않고 "추천 경로 / 고속도로 우선 / 무료도로 우선(톨게이트 피하기)" 중 하나를 고르는 팝업이 뜹니다. 각 경로의 예상 소요시간이 함께 표시됩니다.

---

## 5. 집·회사·즐겨찾기 빠른 목적지

상단바에 집·회사 아이콘이, 검색 이력 팝업 안에 즐겨찾기(하트, 기본 5칸)가 있습니다. 설정 화면의 "즐겨찾기 표시 개수"에서 -/+ 버튼으로 0~5개까지 원하는 만큼 줄이거나 늘릴 수 있습니다. 등록 안 된 칸/등록된 칸에 따라 동작이 다릅니다.

| 상태 | 짧게 누르기 | 길게 누르기 |
|---|---|---|
| 등록 안 됨 | 검색해서 고르면 그 자리에 등록 | 위와 동일 |
| 등록됨 | 이동 방식이 저장돼 있으면 그 방식으로 바로 안내 시작, 저장 안 돼 있으면 매번 "추천/고속도로/무료도로" 물어봄(고른 건 이번 한 번만, 자동 저장 안 됨) | "다시 검색 / 이름 변경 / 이동방식 저장 / 삭제 / 취소" 선택창 |

- 즐겨찾기는 등록되면 하트 대신 등록된 장소 이름이 표시되고, 그 밑에 현재 위치 기준 예상 소요시간("OO분")이 함께 표시됩니다. 이 소요시간은 검색 이력 팝업을 열 때마다 실제 카카오 경로 계산으로 새로 구합니다.
- "이름 변경"은 저장된 주소는 그대로 두고 화면에 표시되는 이름만 바꿉니다.
- "삭제"를 선택하면 등록이 해제되어 다시 빈 칸(하트/미등록)으로 돌아갑니다.

---

## 6. 안내 이어가기

길안내 중에 앱을 업데이트하거나 강제 종료 후 다시 켜도, 앱을 다시 열면 "OO(으)로 가던 중이었어요. 이어서 안내할까요?"라고 물어봅니다. "이어서 안내"를 누르면 바로 그 목적지로 안내가 재개되고, "아니요"를 누르면 그냥 무시하고 평소처럼 새로 검색해서 쓸 수 있습니다.

- 목적지에 정상적으로 도착하거나, "안내종료" 버튼으로 직접 끈 경우에는 저장 정보가 자동으로 지워집니다.
- 앱이 업데이트나 강제종료로 인해 이 "끄는 절차"를 거치지 못하고 갑자기 죽었을 때만 저장 정보가 남아있어서, 다음에 앱을 열 때 물어봅니다.

---

## 7. 카메라·구간단속 등 안전정보 우선순위

TmapNda는 Tmap과 카카오 양쪽에서 안전정보(과속카메라·구간단속·방지턱·스쿨존 등)를 동시에 받아서, 아래 우선순위로 openpilot에 전달합니다.

1. **카카오 길안내 중이고, 그 값이 검증(trusted)됐으면 카카오 값을 우선 채택**
2. **카카오 값이 없거나 검증 안 됐을 때는 Tmap이 감지한 정보를 대신 사용**

카카오 쪽 정보는 경로 기반으로 남은 거리가 정확히 검증된 경우에만 실제로 openpilot에 전달됩니다(직선거리 대신, 실제 안내 경로를 따라간 거리로 계산해서 곡선 도로에서도 정확도를 높였습니다). 검증에 실패한 경우는 감속에 쓰지 않고 로그로만 남습니다.

---

## 문제 발생 시 (에러/불편함 신고)

1. TmapNda 상단바 **로그 전송** 버튼 클릭
2. **이메일** 선택 → 등록된 주소로 자동 입력됨 (메일 앱이 열리면 직접 **보내기**를 눌러야 실제로 전송됩니다)
3. **로그 파일만 첨부**해서 전송 (증상을 간단히 본문에 적어주시면 확인이 훨씬 빠릅니다)

필요하면 **≡ 메뉴 → 로그 전체 삭제**로 쌓인 로그를 한 번에 정리할 수 있습니다 (로그는 실행 중 계속 쌓이다가 10MB가 넘으면 자동으로 회전됩니다).

---

## 업데이트 내역

최신 버전은 [Releases 페이지](https://github.com/Rusiperso/TmapNda/releases)에서 APK로 받을 수 있습니다. (지난 버전들의 변경 내용은 Releases 페이지에서 확인할 수 있습니다.)

### v13.6

1. 즐겨찾기 칸을 넓혀서 시간이 잘리던 문제를 고쳤습니다.
2. 최근검색 목록의 소요시간이 흰색으로만 나오던 문제를 고쳤습니다(노란색으로 강조).
3. 추천 경로와 무료도로 우선 경로의 거리가 거의 같으면(=톨게이트가 없는 구간), 경로 선택 팝업 없이 바로 추천 경로로 안내를 시작합니다.
4. 카카오 안내 중 대안경로 합류 지점에서 불필요하게 감속이 걸리던 문제를 고쳤습니다.
5. 경로를 선택하고 안내를 시작할 때의 딜레이를 줄이기 위해, 미리 계산해둔 경로를 재사용하도록 했습니다.
6. 티맵 차선정보 갱신 문제, 카메라 알림음을 끄는 설정이 있는지를 확인하기 위한 진단 로그를 추가했습니다.




---

## 커뮤니티

사용법 질문, 버그 공유, 업데이트 소식은 카카오톡 오픈채팅방에서 확인할 수 있습니다.

- 오픈채팅: https://open.kakao.com/o/gORB6tFi
- 입장 비밀번호: `134679`

---

## 연결 안 될 때 체크리스트

| 확인 항목 | 방법 |
|---|---|
| Tmap 앱키 입력 여부 | ≡ 메뉴 → **설정을 길게 누르면** 키 재입력 화면으로 이동, 키가 채워져 있는지 확인 |
| 카카오 키 입력 여부 | 목적지 검색·길안내가 안 되면 REST API 키/네이티브 앱 키가 채워져 있는지 확인 |
| 같은 네트워크 여부 | TmapNda 실행 폰과 콤마가 같은 네트워크(같은 SSID)에 붙어있는지 확인 - 폰에 저장된 다른 와이파이로 자동 전환되지 않았는지도 확인 |
| Comma 화면 표시 | 상단바에 `Comma 연결됨` 표시가 뜨는지 확인 |
| 앱 재실행 | 키 입력 후 TmapNda를 껐다 다시 켰는지 확인 |
| 위 방법대로 해도 안될 시 | 상단바 로그 전송 버튼으로 로그 보내기 (증상을 간단히 적어주세요) |
